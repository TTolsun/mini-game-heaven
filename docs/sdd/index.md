# mini-game-heaven SDD

**수정할 기능이 있으면 [시스템 개요](overview.md)의 패키지 표에서 시작하세요.** 이 사이트는 `camera-hal-sdd` 파이프라인이 `native/` C++ 소스에서 생성한 설계 문서와, 사람이 기록한 설계 결정을 나눠서 담습니다.

| 지금 확인할 내용 | 이동할 문서 |
|---|---|
| 수정할 패키지와 진입점을 찾습니다. | [시스템 개요](overview.md) |
| 클래스의 책임과 상속 관계를 확인합니다. | [컴포넌트 구조와 책임](components.md) |
| 특정 동작의 호출 순서를 따라갑니다. | [핵심 시나리오](scenarios/index.md) |
| 동작이 어떤 `-D` 플래그나 `#if` 아래에 있는지 확인합니다. | [컴파일 플래그 매트릭스](feature-flags.md) |
| 게임 스레드와 오디오 콜백 사이를 넘는 코드를 고칩니다. | [스레드와 콜백 모델](threading.md) |
| 바꾸면 안 되는 규칙을 확인합니다. | [변경 시 지켜야 할 제약](constraints.md) |
| 왜 이렇게 만들었는지 확인합니다. | [설계 결정 기록](decisions.md) |

## 문서를 읽을 때 구분할 것

"코드로 확인한 구조" 아래 문서는 compile DB(arm64-v8a Debug, CMake 구성)를 기준으로 정적 분석한 사실만 담습니다. 기기에서 실행해 본 결과가 아닙니다. "사람이 기록한 설계" 아래 문서는 파이프라인이 덮어쓰지 않으며, 설계 결정과 기기 관찰을 사람이 씁니다.

각 문서 끝의 "근거와 검토 정보"에서 근거 파일, 인용 검증 결과, 검토 상태를 확인할 수 있습니다.

| frontmatter | 뜻 |
|---|---|
| `status: ok` | 본문의 모든 `파일:줄` 인용이 사실 모델에서 확인되었습니다. |
| `status: needs-review` | 인용 검증에 실패했거나, 정적 분석으로는 알 수 없는 절입니다. 내용을 그대로 믿지 마세요. |
| `facts_omitted` | 모델 입력 예산 때문에 제외된 사실입니다. 그 부분은 문서에 없습니다. |

## 문서를 갱신하는 방법

코드가 바뀌면 `tools/docs-check.sh`가 어느 문서가 오래됐는지 알려 주고, `tools/docs-sync.sh`가 로컬 Qwen으로 영향받은 절만 다시 씁니다. 자세한 절차는 저장소 README의 "문서" 절에 있습니다.

다음 단계: [시스템 개요](overview.md)
